{
  "version": "0.2.0",
  "configurations": [{
    "name": "Persistent Markdown Preview", "type": "extensionHost", "request": "launch",
    "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
    "outFiles": ["${workspaceFolder}/dist/**/*.js"], "preLaunchTask": "Compile extension"
  }]
}
